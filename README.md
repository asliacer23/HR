# HR Management System

## Default accounts

Use these seeded demo accounts after running the HR seed migration:

- Admin: `adminhr@gmail.com` / `admin123`
- HR Lead: `hrlead@bestlink.local` / `admin123`
- Registrar-linked demo user: `clara.registrar@bestlink.local` / `admin123`

All additional demo accounts in the HR seed use the same password: `admin123`.
